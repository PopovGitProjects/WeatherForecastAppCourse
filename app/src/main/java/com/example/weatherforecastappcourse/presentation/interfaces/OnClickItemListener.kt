package com.example.weatherforecastappcourse.presentation.interfaces

import com.example.weatherforecastappcourse.models.WeatherModel

interface OnClickItemListener {
    fun onClickItem(item: WeatherModel)
}